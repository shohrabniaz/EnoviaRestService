/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.bjit.ewc18x.bom.model;

import java.util.*;
/**
 *
 * @author Ashikur Rahman
 */
public class DummyBOM {
    /*private List<Obj> listOfAttributes() {
        List<Obj> objs = new ArrayList<>();
        // object 1
        List<Attr> attributes = new ArrayList<>();
        attributes.add(new Attr("name", "ID001"));
        attributes.add(new Attr("inventory", "m3"));
        objs.add(new Obj(attributes));
        // object 2
        List<Attr> attributes2 = new ArrayList<>();
        attributes2.add(new Attr("name", "ID002"));
        attributes2.add(new Attr("inventory", "m3"));
        objs.add(new Obj(attributes2));
        return objs;
    }
    
    private List<Obj> listOfSummaryAttributes() {
        List<Obj> objs = new ArrayList<>();
        // object 1
        List<Attr> attributes = new ArrayList<>();
        attributes.add(new Attr("language", "English"));
        attributes.add(new Attr("commodity code", "ABC"));
        objs.add(new Obj(attributes));
        // object 2
        List<Attr> attributes2 = new ArrayList<>();
        attributes2.add(new Attr("language", "English"));
        attributes2.add(new Attr("commodity code", "ABC"));
        objs.add(new Obj(attributes2));
        return objs;
    }
    
    private List<Data> listOfMetaDataInfo() {
        List<Data> data = new ArrayList<>();
        data.add(new Data("name", "ash"));
        data.add(new Data("date", new Date().toString()));
        return data;
    }
    
    private ObjectTree generateObjTree() {
        return new ObjectTree(listOfAttributes());
    }
    
    private SummaryReport generateSummaryReport() {
        return new SummaryReport(listOfSummaryAttributes());
    }
    
    private MetaData generateMetaData() {
        return new MetaData(listOfMetaDataInfo());
    }
    
    private Report getReport() {
        return new Report("ëbom", generateObjTree(), generateSummaryReport(), generateMetaData());
    }
    
    public GenerateReport generateReport() {
        return new GenerateReport(getReport());
    }*/
}
